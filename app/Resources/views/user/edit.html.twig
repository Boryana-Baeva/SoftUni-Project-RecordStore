{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="col-sm-6 col-sm-offset-3">
            <div class="panel panel-default" style="width: 100%">
                <div class="panel-body">
                    <h1 class="text-center" style="color: #FE980F">Profile Edit</h1>
                    {{ form_start(edit_profile_form) }}
                    {{ form_widget(edit_profile_form) }}
                    <div class="text-center">
                        <button class="btn btn-primary" type="submit" value="Edit">Edit</button>
                    </div>
                    {{ form_end(edit_profile_form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}

