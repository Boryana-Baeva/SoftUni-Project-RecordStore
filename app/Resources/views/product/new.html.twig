{% extends 'base.html.twig' %}

{% block body %}
    <h1 style="text-align: center">Product creation</h1>
    <div class="container">
        <div class="col-sm-6 col-sm-offset-3">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <input type="submit" value="Create"/>
            {{ form_end(form) }}

            <ul>
                <li>
                    <a href="{{ path('product_index') }}">Back to the list</a>
                </li>
            </ul>
        </div>
    </div>
{% endblock %}
